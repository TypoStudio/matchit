# 에이전트 개발 가이드: 새로운 학습 문제 추가하기

이 문서는 다른 AI 에이전트가 이 프로젝트에 새로운 학습 문제를 추가하는 방법을 안내합니다.

## 1. 프로젝트 개요

이 프로젝트는 사용자가 화학식, 문법, 공식, 사자성어와 같은 학습 개념을 블럭 퍼즐 규칙에 맞춰 배우는 정적 웹 게임입니다. 모든 학습 데이터는 JSON 파일로 관리되므로, 새로운 콘텐츠를 쉽게 추가하고 확장할 수 있습니다.

## 2. 데이터 구조

학습 데이터는 `public/data/` 디렉토리 아래에 다음과 같은 구조로 구성되어 있습니다.

- **`public/data/lesson-packs.json`**: 사용 가능한 모든 학습팩의 목록을 담고 있는 마스터 파일입니다. 각 팩의 `items` 데이터는 포함하지 않고, 메타데이터와 사용 가능한 레벨 목록만 가집니다.

  ```json
  [
    {
      "id": "science-core",
      "title": "과학 기초",
      "accent": "#14b8a6",
      "levels": [1, 2, 3]
    },
    {
      "id": "korean-idiom",
      "title": "사자성어",
      "accent": "#f97316",
      "levels": [1, 2, 3]
    }
  ]
  ```

- **`public/data/packs/{pack-id}/{level}.json`**: 각 학습팩의 특정 레벨에 해당하는 문제들이 배열 형태로 저장된 파일입니다.
  - `{pack-id}`: `lesson-packs.json` 에 정의된 팩의 `id` 입니다 (예: `science-core`).
  - `{level}.json`: 해당 팩의 레벨 번호입니다 (예: `1.json`).

  각 레벨 파일 (`{level}.json`) 은 다음과 같은 `LessonItem` 객체의 배열을 포함합니다.

  ```json
  [
    {
      "id": "water",
      "label": "물",
      "prompt": "H2O 만들기",
      "tokens": ["H", "H", "O"],
      "hint": "수소 2개와 산소 1개"
    },
    {
      "id": "salt",
      "label": "소금",
      "prompt": "NaCl 만들기",
      "tokens": ["Na", "Cl"],
      "hint": "나트륨과 염소"
    }
  ]
  ```

## 3. 새로운 문제 추가하는 방법

### 3.1. 기존 팩에 문제 추가하기

1.  **파일 찾기**: 문제를 추가하려는 학습팩의 ID와 레벨을 확인합니다. 예를 들어, '과학 기초' 팩의 레벨 1에 문제를 추가하려면 `public/data/packs/science-core/1.json` 파일을 엽니다.
2.  **`LessonItem` 추가**: 파일 내의 JSON 배열에 새로운 `LessonItem` 객체를 추가합니다. 객체는 다음 필드를 가져야 합니다.
    - `id`: 문제의 고유한 식별자 (문자열, 영어와 하이픈(-) 사용).
    - `label`: 문제의 이름 (예: "암모니아").
    - `prompt`: 사용자에게 보여줄 질문 (예: "NH3 만들기").
    - `tokens`: 정답을 구성하는 토큰(블럭)의 배열. 게임 보드에 이 순서대로 나타납니다.
    - `hint`: 사용자에게 제공할 힌트.
3.  **저장**: 파일을 저장합니다.

### 3.2. 기존 팩에 새로운 레벨 추가하기

1.  **새 레벨 파일 생성**: `public/data/packs/{pack-id}/` 디렉토리 안에 `{new-level}.json` 형식으로 새 파일을 생성합니다. 예를 들어 '과학 기초' 팩에 레벨 4를 추가하려면 `public/data/packs/science-core/4.json` 파일을 생성합니다.
2.  **문제 추가**: 새로 만든 JSON 파일에 `LessonItem` 객체의 배열을 추가합니다.
3.  **마스터 파일 업데이트**: `public/data/lesson-packs.json` 파일을 엽니다.
4.  **레벨 번호 추가**: 해당 팩의 `levels` 배열에 새로 추가한 레벨 번호를 추가합니다.

    ```json
    // 변경 전
    "levels": [1, 2, 3]
    // 변경 후
    "levels": [1, 2, 3, 4]
    ```
5.  **저장**: 파일을 저장합니다.

### 3.3. 새로운 팩 추가하기

1.  **새 디렉토리 생성**: `public/data/packs/` 아래에 새 팩의 ID로 디렉토리를 생성합니다 (예: `public/data/packs/new-awesome-pack`).
2.  **레벨 파일 생성**: 새로 만든 디렉토리 안에 최소 하나 이상의 레벨 파일 (예: `1.json`)을 생성하고 `LessonItem` 배열로 문제들을 추가합니다.
3.  **마스터 파일 업데이트**: `public/data/lesson-packs.json` 파일을 엽니다.
4.  **새 팩 정보 추가**: JSON 배열에 새로운 `LessonPack` 객체를 추가합니다.

    ```json
    {
      "id": "new-awesome-pack",
      "title": "새로운 멋진 팩",
      "accent": "#8B5CF6", // 팩의 고유 색상
      "levels": [1] // 생성한 레벨 목록
    }
    ```
5.  **저장**: 파일을 저장합니다.

## 4. 변경 사항 테스트하기

프로젝트를 로컬에서 실행하여 추가하거나 수정한 내용이 올바르게 표시되고 게임이 정상적으로 동작하는지 확인해야 합니다.

1.  **의존성 설치**:
    ```bash
    npm install
    ```
2.  **개발 서버 실행**:
    ```bash
    npm run dev
    ```
3.  브라우저에서 표시되는 주소(일반적으로 `http://localhost:5173`)로 접속하여 게임을 테스트합니다.
